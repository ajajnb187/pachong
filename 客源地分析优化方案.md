# 客源地分析优化方案

## 📊 现状分析

### 当前数据源
- **Hive表**: `fuzhou_reviews` 
- **可用字段**: `city`（城市）、`visitor_name`（游客昵称）
- **数据量**: 90条评论，73个独立游客
- **问题**: city字段数据量少，存在中文乱码

### 核心问题
**旅游景区客流量和客源地数据无法通过公开渠道直接获取**，只能通过以下方式推算：
1. ✅ 基于评论数据推算（当前方案）
2. 🔄 集成第三方人口地理数据API（需付费）
3. 🆕 扩展爬虫数据源（需开发）

---

## 🎯 解决方案

### 方案一：优化现有评论数据分析 ⭐推荐

**优势**：不影响现有系统，零成本，立即可用

#### 1. 优化SQL统计逻辑
```sql
-- 客源地分析：基于评论数据统计独立游客来源城市
SELECT 
    city as source_city,
    COUNT(DISTINCT visitor_name) as unique_visitors,  -- 独立游客数
    COUNT(*) as review_count,                         -- 评论总数
    ROUND(COUNT(DISTINCT visitor_name) * 100.0 / 
          SUM(COUNT(DISTINCT visitor_name)) OVER(), 2) as percentage
FROM tourism_db.fuzhou_reviews
WHERE city IS NOT NULL AND city != ''
GROUP BY city
ORDER BY unique_visitors DESC
LIMIT 10;
```

#### 2. 前端展示说明
在客源地分析页面添加数据说明：
```
📌 数据来源说明：
客源地数据基于评论用户的城市信息统计，代表留下评论的游客来源分布。
实际客流量可能更大，此数据作为参考指标。
```

#### 3. 已修复的代码
- ✅ `ScenicAnalysisServiceImpl.java` 第687-696行
  - 修正 `cityDistribution` 使用 `city` 字段
  - 统一字段映射逻辑

---

### 方案二：集成百度慧眼API 🌟企业级方案

**适用场景**：需要真实客流量数据的商业项目

#### API服务：百度地图慧眼 - 城市人口地理大数据
- **官网**: https://huiyan.baidu.com/products/platform
- **功能**：
  - ✅ 实时/历史客流量查询
  - ✅ 客流来源行政区/小区分析
  - ✅ 客流画像（年龄、性别、消费能力）
  - ✅ 热力图可视化
  - ✅ 支持自定义区域圈选（景区边界）

#### 集成步骤
1. **商务对接**: 联系百度慧眼团队获取API权限
2. **技术集成**: 
   - 在 `tourism-flask/config.py` 添加配置
   ```python
   # 百度慧眼API配置
   BAIDU_HUIYAN_API_KEY = 'your_api_key'
   BAIDU_HUIYAN_API_URL = 'https://huiyan.baidu.com/api/v1'
   ```
   
3. **创建新爬虫**: `tourism-flask/crawlers/baidu_huiyan_crawler.py`
   ```python
   """
   百度慧眼客流数据爬虫
   获取福州景区客流量和客源地分析数据
   """
   import requests
   from config import Config
   
   class BaiduHuiyanCrawler:
       def __init__(self):
           self.api_key = Config.BAIDU_HUIYAN_API_KEY
           self.api_url = Config.BAIDU_HUIYAN_API_URL
       
       def get_scenic_visitor_flow(self, scenic_name, lat, lng, radius=1000):
           """
           获取景区客流量数据
           
           Args:
               scenic_name: 景区名称
               lat: 纬度
               lng: 经度
               radius: 统计半径（米）
           """
           # API调用逻辑
           pass
       
       def get_visitor_source_distribution(self, scenic_name, date):
           """
           获取景区客源地分布
           
           Returns:
               {
                   'total_visitors': 总客流量,
                   'source_cities': [
                       {'city': '城市', 'count': 人数, 'percentage': 百分比}
                   ]
               }
           """
           # API调用逻辑
           pass
   ```

4. **Hive表扩展**: 新增客流量数据表
   ```sql
   -- 景区客流量数据表（来自百度慧眼API）
   CREATE EXTERNAL TABLE scenic_visitor_flow (
       scenic_spot STRING COMMENT '景区名称',
       city STRING COMMENT '城市',
       flow_date STRING COMMENT '统计日期',
       visitor_count INT COMMENT '客流量',
       source_city STRING COMMENT '客源城市',
       source_count INT COMMENT '客源人数',
       data_source STRING COMMENT '数据来源：baidu_huiyan',
       crawl_time STRING COMMENT '爬取时间'
   )
   PARTITIONED BY (dt STRING COMMENT '爬取时间戳')
   ROW FORMAT SERDE 'org.apache.hive.hcatalog.data.JsonSerDe'
   STORED AS TEXTFILE
   LOCATION '/tourism/data/visitor_flow/';
   ```

#### 成本评估
- **商务成本**: 需要联系百度商务，通常按查询次数或数据量收费
- **开发成本**: 约2-3个工作日完成集成
- **数据质量**: ⭐⭐⭐⭐⭐ 真实权威的人口流动数据

---

### 方案三：集成BestTime API 🌍国际方案

**适用场景**：全球范围景区分析（非中国市场）

#### API服务：BestTime.app 人流量预测API
- **官网**: https://www.explinks.com/api/scd20240725051606649337
- **覆盖**: 150+国家公共场所
- **功能**：
  - 人流量预测（按小时）
  - 实时拥挤度监测
  - 历史客流量数据

#### 适用性分析
- ❌ **不适合福州景区**: BestTime主要基于国外手机信号数据
- ⚠️ **中国覆盖有限**: 国内景区数据可能缺失或不准确
- ✅ **适合国际项目**: 如分析巴黎卢浮宫、纽约自由女神等

---

### 方案四：扩展爬虫数据源 🔧开发方案

#### 可扩展的数据源

**1. 携程评论扩展**（当前已实现）
- 优化 `city` 字段提取逻辑
- 从评论内容中提取地理位置信息
```python
# 从评论内容中提取城市信息
def extract_city_from_review(review_content):
    """
    示例：
    "从北京来的，风景很美" -> 北京
    "上海游客路过" -> 上海
    """
    cities = ['北京', '上海', '广州', '深圳', ...]
    for city in cities:
        if city in review_content:
            return city
    return None
```

**2. 美团/大众点评评论**
- 扩展爬虫支持多平台
- 交叉验证数据准确性

**3. 马蜂窝游记数据**
- 游记作者地理位置
- 更准确的客源地分析

#### 实施建议
不修改现有 `ctrip_crawler_v2.py`，新增独立爬虫模块：
```python
# tourism-flask/crawlers/multi_platform_crawler.py
class MultiPlatformCrawler:
    """
    多平台评论数据聚合爬虫
    不影响现有携程爬虫，独立运行
    """
    def __init__(self):
        self.ctrip = CtripCrawlerV2()
        self.meituan = MeituanCrawler()  # 新增
        self.mafengwo = MafengwoCrawler()  # 新增
```

---

## 💡 推荐实施路线

### 阶段一：立即可用（0成本）✅
1. ✅ 已修复 `getVisitorSource` 的SQL逻辑
2. ✅ 优化统计方式：独立游客数而非评论数
3. 📝 前端添加数据来源说明
4. 🔍 优化评论内容中的城市信息提取

### 阶段二：数据扩充（1-2周）
1. 优化携程爬虫的city字段提取
2. 扩展其他平台评论数据源
3. 交叉验证提升数据覆盖率

### 阶段三：商业API集成（需预算）
1. 评估项目需求和预算
2. 对接百度慧眼或其他人口地理数据服务
3. 建立双数据源体系：免费+付费

---

## 📋 当前已修复内容

### 1. 景区排行榜SQL优化
```java
// 游客流量：使用 COUNT(DISTINCT visitor_name) 统计独立游客
if ("visitor".equals(rankType)) {
    sql = "SELECT scenic_spot, " +
          "COUNT(DISTINCT visitor_name) as visitor_count " +
          "FROM tourism_db.fuzhou_reviews " +
          "GROUP BY scenic_spot " +
          "ORDER BY visitor_count DESC LIMIT " + limit;
}
```

### 2. 客源地分析SQL修复
```java
// 使用 city 字段统计城市分布
String sql = "SELECT city, COUNT(*) as visitor_count " +
             "FROM tourism_db.fuzhou_reviews " +
             "WHERE city IS NOT NULL " +
             "GROUP BY city ORDER BY visitor_count DESC LIMIT 10";
```

### 3. 字段映射修复
```java
// cityDistribution 正确使用 city 字段
List<Map<String, Object>> cityDistribution = locationData.stream()
    .map(m -> {
        Map<String, Object> city = new HashMap<>();
        String cityName = m.get("city") != null ? m.get("city").toString() : "未知";
        city.put("city", cityName);
        city.put("source_city", cityName);
        city.put("visitor_count", m.get("visitor_count"));
        return city;
    }).collect(Collectors.toList());
```

---

## ⚠️ 数据质量现状

根据Hive查询结果：
```
+-------+---------------+------------------+
| city  | review_count  | unique_visitors  |
+-------+---------------+------------------+
| ??    | 90            | 73               |
+-------+---------------+------------------+
```

**问题**：
- city字段中文乱码（显示为??）
- 数据覆盖率低（仅90条评论）

**建议**：
1. 检查爬虫编码配置
2. 增加爬取数据量（当前max_reviews=50，可增加到100+）
3. 验证JSON序列化是否正确处理中文

---

## 🎬 下一步行动

### 立即执行
1. ✅ 重启SpringBoot服务，验证修复效果
2. 🔍 检查爬虫中文编码问题
3. 📈 增加评论爬取数量

### 短期计划（本周）
1. 优化评论数据中的城市信息提取
2. 前端添加数据来源说明
3. 验证数据统计准确性

### 中期计划（1-2周）
1. 扩展多平台评论数据源
2. 优化Hive数据质量

### 长期计划（需评估）
1. 评估商业API需求
2. 预算允许时集成百度慧眼API
3. 建立混合数据源架构
