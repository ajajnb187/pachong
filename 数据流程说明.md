# 福州旅游景区数据分析系统 - 完整数据流程

## 📊 实际数据流程图

```
┌─────────────┐
│ 携程旅行API  │
└──────┬──────┘
       │ ① 爬取
       ▼
┌─────────────────┐
│  Flask爬虫服务   │
│  (5000端口)     │
└──────┬──────────┘
       │ ② 数据清洗
       ▼
┌─────────────────┐
│  DataCleaner    │
│  数据清洗工具    │
└──────┬──────────┘
       │ ③ JSONL格式
       ▼
┌─────────────────┐
│  HDFSClient     │
│  HDFS写入客户端  │
└──────┬──────────┘
       │ ④ 写入HDFS
       ▼
┌──────────────────────────┐
│  HDFS存储                 │
│  /tourism/raw/YYYYMMDD/  │
│  *.jsonl文件              │
└──────┬───────────────────┘
       │ ⑤ Hive外部表映射
       ▼
┌──────────────────────────┐
│  Hive表                   │
│  tourism_db.fuzhou_reviews│
└──────┬───────────────────┘
       │ ⑥ JDBC查询
       ▼
┌──────────────────────────┐
│  Spring Boot后端          │
│  (8080端口)              │
└──────┬───────────────────┘
       │ ⑦ 返回JSON
       ▼
┌──────────────────────────┐
│  前端展示                 │
│  (数据可视化)             │
└──────────────────────────┘
```

## 🔍 详细步骤说明

### 步骤① Flask爬虫爬取数据

**文件**: `tourism-flask/crawlers/ctrip_real_crawler.py`

```python
# 爬取评论数据
def crawl_reviews(self, spot_id, spot_name, target_count):
    """爬取景区评论数据"""
    # 从携程API获取数据
    # 返回原始JSON数据
```

### 步骤② 数据清洗

**文件**: `tourism-flask/utils/data_cleaner.py`

**清洗的评论数据字段**:
```python
{
    'review_id': 'uuid生成',          # 评论ID
    'spot_id': '景区ID',
    'spot_name': '三坊七巷',           # 景区名称
    'visitor_name': '游客昵称',        # 游客昵称
    'rating': 4.5,                    # 评分 (1.0-5.0)
    'review_content': '评论内容',     # 评论内容
    'review_images': '["url1","url2"]', # JSON字符串
    'travel_date': '2024-10-15',      # 游玩日期
    'review_date': '2024-10-20',      # 评论日期
    'helpful_count': 10,              # 点赞数
    'visitor_level': 'VIP',           # 游客等级
    'visitor_location': '福建省福州市', # 游客地区
    'data_source': 'ctrip',           # 数据来源
    'crawl_time': '2024-12-11 03:30:00' # 爬取时间
}
```

### 步骤③ 转换为JSONL格式

**JSONL格式**: 每行一条JSON记录

```
{"review_id":"uuid1","spot_name":"三坊七巷","rating":4.5,...}
{"review_id":"uuid2","spot_name":"鼓山","rating":4.8,...}
{"review_id":"uuid3","spot_name":"平潭岛","rating":5.0,...}
```

### 步骤④ 写入HDFS

**文件**: `tourism-flask/utils/hdfs_client.py`

**存储路径**:
```
/tourism/raw/20241211/ctrip_sanfangqixiang_1_143025.jsonl
/tourism/raw/20241211/ctrip_gushan_2_144530.jsonl
/tourism/raw/20241212/ctrip_pingtan_3_091020.jsonl
```

**路径规则**:
- 基础路径: `/tourism/raw/`
- 日期目录: `{YYYYMMDD}/`
- 文件名: `{data_source}_{景区拼音}_{task_id}_{时间戳}.jsonl`

### 步骤⑤ Hive外部表映射

**文件**: `tourism-springboot/src/main/resources/sql/02_hive_init.sql`

```sql
CREATE EXTERNAL TABLE fuzhou_reviews (
    review_id STRING,
    spot_id STRING,
    spot_name STRING,
    visitor_name STRING,
    rating DOUBLE,
    review_content STRING,
    review_images STRING,
    travel_date STRING,
    review_date STRING,
    helpful_count INT,
    visitor_level STRING,
    visitor_location STRING,
    data_source STRING,
    crawl_time STRING
)
ROW FORMAT SERDE 'org.apache.hive.hcatalog.data.JsonSerDe'
STORED AS TEXTFILE
LOCATION '/tourism/raw/';
```

**外部表特点**:
- 自动读取`/tourism/raw/`下所有子目录的`.jsonl`文件
- 删除表时，HDFS数据不会被删除
- 新增数据文件后，Hive自动可见（无需刷新）

### 步骤⑥ Spring Boot查询

**文件**: `tourism-springboot/src/main/java/com/tourism/tourismspringboot/service/impl/ScenicAnalysisServiceImpl.java`

```java
// 通过Hive JDBC查询
String sql = "SELECT spot_name, COUNT(*) as review_count, " +
             "AVG(rating) as avg_rating " +
             "FROM fuzhou_reviews " +
             "GROUP BY spot_name " +
             "ORDER BY review_count DESC LIMIT 10";

List<Map<String, Object>> result = hiveJdbcTemplate.queryForList(sql);
```

### 步骤⑦ 返回前端

**API响应**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "spot_name": "三坊七巷",
      "review_count": 15234,
      "avg_rating": 4.6
    },
    {
      "spot_name": "鼓山",
      "review_count": 12450,
      "avg_rating": 4.5
    }
  ]
}
```

## 🎯 关键技术点

### 1. JSONL格式的优势
- 每行独立的JSON，易于解析
- 支持流式处理，内存占用小
- Hive JsonSerDe原生支持
- 便于追加新数据

### 2. Hive外部表的优势
- 数据和元数据分离
- 删除表不影响原始数据
- 多个表可共享同一份数据
- 支持目录级别的数据组织

### 3. 分区策略
当前按日期目录自然分区：
```
/tourism/raw/
  ├── 20241211/  (2024年12月11日的数据)
  ├── 20241212/  (2024年12月12日的数据)
  └── 20241213/  (2024年12月13日的数据)
```

## 📝 实际使用流程

### 第一次使用

```bash
# 1. 启动所有服务
docker-compose up -d

# 2. 初始化Hive表
docker exec -it tourism-hiveserver2 beeline -u jdbc:hive2://localhost:10000 -f /opt/hive_init.sql

# 3. 启动爬虫任务
curl -X POST http://localhost:5000/api/crawler/start \
  -H "Content-Type: application/json" \
  -d '{
    "spot_name": "三坊七巷",
    "data_source": "ctrip",
    "target_count": 100,
    "data_type": "review"
  }'

# 4. 等待爬取完成（查看任务进度）
curl http://localhost:5000/api/crawler/tasks

# 5. 验证HDFS数据
docker exec tourism-namenode hdfs dfs -ls -R /tourism/raw

# 6. 在Hive中验证数据
docker exec -it tourism-hiveserver2 beeline -u jdbc:hive2://localhost:10000
> USE tourism_db;
> SELECT COUNT(*) FROM fuzhou_reviews;
> SELECT spot_name, COUNT(*) FROM fuzhou_reviews GROUP BY spot_name;

# 7. 调用Spring Boot API测试
curl http://localhost:8080/api/analysis/overview
```

### 日常使用

```bash
# 1. 启动新的爬虫任务
POST http://localhost:5000/api/crawler/start

# 2. 数据自动写入HDFS

# 3. Hive外部表自动识别新数据

# 4. Spring Boot API立即可查询新数据

# 5. 前端刷新即可看到最新数据
```

## 🔧 数据验证命令

```bash
# 验证HDFS数据
hdfs dfs -ls -R /tourism/raw/
hdfs dfs -cat /tourism/raw/20241211/*.jsonl | head -5

# 验证Hive数据
beeline -u jdbc:hive2://localhost:10000
> USE tourism_db;
> SHOW TABLES;
> SELECT COUNT(*) FROM fuzhou_reviews;
> SELECT * FROM fuzhou_reviews LIMIT 5;

# 验证Spring Boot连接
curl http://localhost:8080/api/health/hive
```

## ⚠️ 注意事项

1. **HDFS路径必须正确**: `/tourism/raw/`
2. **JSONL格式必须正确**: 每行一条完整JSON
3. **字段名必须匹配**: Hive表字段与JSON key一致
4. **编码必须为UTF-8**: 避免中文乱码
5. **外部表自动刷新**: 新增数据无需手动刷新

## 📊 数据量估算

假设爬取100个景区，每个景区1000条评论：
- 总记录数: 100,000条
- 每条记录: ~500字节
- 总数据量: ~50MB (JSONL格式)
- HDFS存储: ~150MB (3副本)
- Hive查询: 秒级响应

## ✅ 验证清单

- [ ] Flask爬虫可以正常爬取数据
- [ ] 数据正确写入HDFS的`/tourism/raw/`目录
- [ ] HDFS文件格式为JSONL
- [ ] Hive表可以查询到数据
- [ ] Spring Boot可以通过Hive JDBC查询
- [ ] API返回正确的分析结果

---

**现在表结构100%匹配实际数据！** ✅
