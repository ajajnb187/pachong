# 福州旅游景区数据分析与可视化系统 - 前端功能模块设计

## 一、前端技术栈

### 1.1 核心技术
- **框架**：Vue 3.x (Composition API)
- **构建工具**：Vite 4.x
- **UI组件库**：Element Plus
- **图表库**：ECharts 5.x
- **HTTP客户端**：Axios
- **路由**：Vue Router 4.x
- **状态管理**：Pinia
- **工具库**：Day.js、Lodash

### 1.2 开发工具
- **代码编辑器**：VS Code
- **包管理器**：npm / yarn
- **代码规范**：ESLint + Prettier

---

## 二、页面结构设计

### 2.1 路由结构

```javascript
const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/Login.vue')
  },
  {
    path: '/',
    component: () => import('@/layout/MainLayout.vue'),
    redirect: '/dashboard',
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('@/views/Dashboard.vue'),
        meta: { title: '数据概览' }
      },
      {
        path: 'crawler',
        name: 'Crawler',
        component: () => import('@/views/Crawler.vue'),
        meta: { title: '数据采集' }
      },
      {
        path: 'scenic',
        name: 'Scenic',
        component: () => import('@/views/Scenic.vue'),
        meta: { title: '景区管理' }
      },
      {
        path: 'analysis',
        children: [
          {
            path: 'visitor',
            name: 'VisitorAnalysis',
            component: () => import('@/views/analysis/Visitor.vue'),
            meta: { title: '游客数量分析' }
          },
          {
            path: 'review',
            name: 'ReviewAnalysis',
            component: () => import('@/views/analysis/Review.vue'),
            meta: { title: '景区评价分析' }
          },
          {
            path: 'season',
            name: 'SeasonAnalysis',
            component: () => import('@/views/analysis/Season.vue'),
            meta: { title: '季节性分析' }
          },
          {
            path: 'source',
            name: 'SourceAnalysis',
            component: () => import('@/views/analysis/Source.vue'),
            meta: { title: '客源地分析' }
          }
        ]
      },
      {
        path: 'visualization',
        name: 'Visualization',
        component: () => import('@/views/Visualization.vue'),
        meta: { title: '数据大屏' }
      },
      {
        path: 'profile',
        name: 'Profile',
        component: () => import('@/views/Profile.vue'),
        meta: { title: '个人中心' }
      }
    ]
  }
]
```

### 2.2 页面结构图

```
┌─────────────────────────────────────────────────────┐
│                   顶部导航栏                         │
│  Logo  |  系统名称  |              用户信息 [退出]   │
└─────────────────────────────────────────────────────┘
┌──────────┬──────────────────────────────────────────┐
│          │                                          │
│  侧边    │                                          │
│  菜单    │            主内容区域                     │
│          │                                          │
│  - 数据  │                                          │
│    概览  │                                          │
│  - 数据  │                                          │
│    采集  │                                          │
│  - 景区  │                                          │
│    管理  │                                          │
│  - 数据  │                                          │
│    分析  │                                          │
│    ∟游客 │                                          │
│    ∟评价 │                                          │
│    ∟季节 │                                          │
│    ∟客源 │                                          │
│  - 数据  │                                          │
│    大屏  │                                          │
└──────────┴──────────────────────────────────────────┘
```

---

## 三、核心页面设计

### 3.1 登录页面 (Login.vue)

#### 3.1.1 页面功能
- 用户登录
- 用户注册
- 表单验证

#### 3.1.2 页面布局

```vue
<template>
  <div class="login-container">
    <div class="login-box">
      <h2>福州旅游景区数据分析系统</h2>
      
      <el-tabs v-model="activeTab">
        <!-- 登录表单 -->
        <el-tab-pane label="登录" name="login">
          <el-form :model="loginForm" :rules="loginRules" ref="loginFormRef">
            <el-form-item prop="username">
              <el-input v-model="loginForm.username" placeholder="用户名" />
            </el-form-item>
            <el-form-item prop="password">
              <el-input v-model="loginForm.password" type="password" placeholder="密码" />
            </el-form-item>
            <el-button type="primary" @click="handleLogin">登录</el-button>
          </el-form>
        </el-tab-pane>
        
        <!-- 注册表单 -->
        <el-tab-pane label="注册" name="register">
          <el-form :model="registerForm" :rules="registerRules" ref="registerFormRef">
            <el-form-item prop="username">
              <el-input v-model="registerForm.username" placeholder="用户名" />
            </el-form-item>
            <el-form-item prop="password">
              <el-input v-model="registerForm.password" type="password" placeholder="密码" />
            </el-form-item>
            <el-form-item prop="email">
              <el-input v-model="registerForm.email" placeholder="邮箱" />
            </el-form-item>
            <el-button type="primary" @click="handleRegister">注册</el-button>
          </el-form>
        </el-tab-pane>
      </el-tabs>
    </div>
  </div>
</template>
```

---

### 3.2 数据概览页面 (Dashboard.vue)

#### 3.2.1 页面功能
- 显示关键指标统计
- 显示热门景区排行
- 显示最新数据动态
- 快捷功能入口

#### 3.2.2 页面布局

```vue
<template>
  <div class="dashboard">
    <!-- 统计卡片 -->
    <el-row :gutter="20">
      <el-col :span="6">
        <el-card>
          <div class="stat-card">
            <div class="stat-icon">🏞️</div>
            <div class="stat-content">
              <div class="stat-title">景区总数</div>
              <div class="stat-value">{{ overview.total_scenic_spots }}</div>
            </div>
          </div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <div class="stat-card">
            <div class="stat-icon">👥</div>
            <div class="stat-content">
              <div class="stat-title">总游客量</div>
              <div class="stat-value">{{ formatNumber(overview.total_visitors) }}</div>
            </div>
          </div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <div class="stat-card">
            <div class="stat-icon">💬</div>
            <div class="stat-content">
              <div class="stat-title">总评价数</div>
              <div class="stat-value">{{ formatNumber(overview.total_reviews) }}</div>
            </div>
          </div>
        </el-card>
      </el-col>
      <el-col :span="6">
        <el-card>
          <div class="stat-card">
            <div class="stat-icon">⭐</div>
            <div class="stat-content">
              <div class="stat-title">平均评分</div>
              <div class="stat-value">{{ overview.avg_rating }}</div>
            </div>
          </div>
        </el-card>
      </el-col>
    </el-row>

    <!-- 图表展示 -->
    <el-row :gutter="20" style="margin-top: 20px;">
      <el-col :span="12">
        <el-card title="近7日游客趋势">
          <div ref="visitorTrendChart" style="height: 300px;"></div>
        </el-card>
      </el-col>
      <el-col :span="12">
        <el-card title="热门景区TOP5">
          <div ref="hotScenicChart" style="height: 300px;"></div>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>
```

---

### 3.3 数据采集页面 (Crawler.vue)

#### 3.3.1 页面功能
- 启动爬取任务
- 查看任务列表
- 查看任务进度
- 停止正在运行的任务
- 删除历史任务

#### 3.3.2 页面布局

```vue
<template>
  <div class="crawler-page">
    <!-- 创建任务 -->
    <el-card title="创建爬取任务">
      <el-form :model="taskForm" :rules="taskRules" ref="taskFormRef" label-width="120px">
        <el-form-item label="任务名称" prop="task_name">
          <el-input v-model="taskForm.task_name" placeholder="请输入任务名称" />
        </el-form-item>
        
        <el-form-item label="景区名称" prop="scenic_spot_name">
          <el-select v-model="taskForm.scenic_spot_name" placeholder="请选择景区">
            <el-option label="三坊七巷" value="三坊七巷" />
            <el-option label="鼓山" value="鼓山" />
            <el-option label="平潭岛" value="平潭岛" />
          </el-select>
        </el-form-item>
        
        <el-form-item label="数据来源" prop="data_source">
          <el-select v-model="taskForm.data_source" placeholder="请选择数据来源">
            <el-option label="携程旅行" value="ctrip" />
            <el-option label="去哪儿网" value="qunar" />
            <el-option label="美团旅游" value="meituan" />
          </el-select>
        </el-form-item>
        
        <el-form-item label="数据类型" prop="data_type">
          <el-radio-group v-model="taskForm.data_type">
            <el-radio label="review">评价数据</el-radio>
            <el-radio label="info">基本信息</el-radio>
            <el-radio label="count">游客数量</el-radio>
          </el-radio-group>
        </el-form-item>
        
        <el-form-item label="爬取数量" prop="target_count">
          <el-input-number v-model="taskForm.target_count" :min="10" :max="10000" />
        </el-form-item>
        
        <el-form-item>
          <el-button type="primary" @click="startCrawl">开始爬取</el-button>
          <el-button @click="resetForm">重置</el-button>
        </el-form-item>
      </el-form>
    </el-card>

    <!-- 任务列表 -->
    <el-card title="任务列表" style="margin-top: 20px;">
      <el-table :data="taskList" style="width: 100%">
        <el-table-column prop="task_id" label="任务ID" width="80" />
        <el-table-column prop="task_name" label="任务名称" width="200" />
        <el-table-column prop="scenic_spot_name" label="景区名称" width="120" />
        <el-table-column prop="data_source" label="数据来源" width="100" />
        <el-table-column prop="target_count" label="目标数量" width="100" />
        <el-table-column prop="actual_count" label="实际数量" width="100" />
        <el-table-column prop="status" label="状态" width="100">
          <template #default="{ row }">
            <el-tag :type="getStatusType(row.status)">{{ getStatusText(row.status) }}</el-tag>
          </template>
        </el-table-column>
        <el-table-column prop="progress" label="进度" width="150">
          <template #default="{ row }">
            <el-progress :percentage="row.progress" />
          </template>
        </el-table-column>
        <el-table-column prop="create_time" label="创建时间" width="180" />
        <el-table-column label="操作" fixed="right" width="150">
          <template #default="{ row }">
            <el-button 
              v-if="row.status === 'running'" 
              type="danger" 
              size="small" 
              @click="stopTask(row.task_id)"
            >
              停止
            </el-button>
            <el-button 
              v-else 
              type="danger" 
              size="small" 
              @click="deleteTask(row.task_id)"
            >
              删除
            </el-button>
          </template>
        </el-table-column>
      </el-table>
      
      <el-pagination
        v-model:current-page="pagination.page"
        v-model:page-size="pagination.page_size"
        :total="pagination.total"
        @current-change="loadTaskList"
        layout="total, prev, pager, next"
      />
    </el-card>
  </div>
</template>
```

---

### 3.4 游客数量分析页面 (analysis/Visitor.vue)

#### 3.4.1 页面功能
- 选择景区和时间范围
- 显示客流量趋势图
- 显示景区对比图
- 节假日影响分析

#### 3.4.2 图表设计

```vue
<template>
  <div class="visitor-analysis">
    <!-- 筛选条件 -->
    <el-card>
      <el-form :inline="true">
        <el-form-item label="景区">
          <el-select v-model="queryParams.spot_id" multiple placeholder="选择景区">
            <el-option v-for="spot in scenicList" :key="spot.spot_id" 
                       :label="spot.spot_name" :value="spot.spot_id" />
          </el-select>
        </el-form-item>
        <el-form-item label="时间范围">
          <el-date-picker
            v-model="dateRange"
            type="daterange"
            range-separator="至"
            start-placeholder="开始日期"
            end-placeholder="结束日期"
          />
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="loadData">查询</el-button>
        </el-form-item>
      </el-form>
    </el-card>

    <!-- 游客趋势图 -->
    <el-card title="游客数量趋势" style="margin-top: 20px;">
      <div ref="trendChart" style="height: 400px;"></div>
    </el-card>

    <!-- 景区对比图 -->
    <el-card title="景区客流量对比" style="margin-top: 20px;">
      <div ref="compareChart" style="height: 400px;"></div>
    </el-card>
  </div>
</template>

<script setup>
import * as echarts from 'echarts'
import { ref, onMounted, nextTick } from 'vue'

// 初始化折线图
const initTrendChart = (dates, seriesData) => {
  const chartDom = trendChart.value
  const myChart = echarts.init(chartDom)
  
  const option = {
    title: { text: '游客数量趋势' },
    tooltip: { trigger: 'axis' },
    legend: { data: seriesData.map(s => s.name) },
    xAxis: {
      type: 'category',
      data: dates
    },
    yAxis: {
      type: 'value',
      name: '游客数量'
    },
    series: seriesData.map(s => ({
      name: s.name,
      type: 'line',
      data: s.data,
      smooth: true
    }))
  }
  
  myChart.setOption(option)
}
</script>
```

---

### 3.5 景区评价分析页面 (analysis/Review.vue)

#### 3.5.1 页面功能
- 评分分布统计
- 评价趋势分析
- 关键词云图
- 评价列表查看

#### 3.5.2 图表设计

**评分分布饼图**：
```javascript
{
  title: { text: '评分分布' },
  tooltip: { trigger: 'item' },
  series: [{
    type: 'pie',
    radius: '50%',
    data: [
      { value: 8500, name: '5星' },
      { value: 4200, name: '4星' },
      { value: 1800, name: '3星' },
      { value: 500, name: '2星' },
      { value: 234, name: '1星' }
    ]
  }]
}
```

**关键词云图**：
```javascript
{
  series: [{
    type: 'wordCloud',
    data: [
      { name: '历史文化', value: 3567 },
      { name: '交通便利', value: 2845 },
      { name: '人多拥挤', value: 1234 }
    ]
  }]
}
```

---

### 3.6 客源地分析页面 (analysis/Source.vue)

#### 3.6.1 页面功能
- 省份分布地图
- 城市TOP10排行
- 客源地占比饼图

#### 3.6.2 地图可视化

```javascript
// 使用ECharts中国地图
{
  title: { text: '客源地分布' },
  tooltip: {
    trigger: 'item',
    formatter: '{b}: {c}人 ({d}%)'
  },
  visualMap: {
    min: 0,
    max: 100000,
    text: ['高', '低'],
    calculable: true
  },
  series: [{
    name: '游客数量',
    type: 'map',
    map: 'china',
    data: [
      { name: '福建', value: 680000 },
      { name: '广东', value: 125000 },
      { name: '浙江', value: 98000 }
    ]
  }]
}
```

---

### 3.7 数据大屏页面 (Visualization.vue)

#### 3.7.1 页面功能
- 全屏展示数据大屏
- 实时数据更新
- 多维度数据可视化

#### 3.7.2 布局设计

```vue
<template>
  <div class="data-screen">
    <!-- 顶部标题 -->
    <div class="screen-header">
      <h1>福州旅游景区数据分析大屏</h1>
      <div class="time">{{ currentTime }}</div>
    </div>

    <!-- 主体内容 -->
    <div class="screen-content">
      <!-- 左侧 -->
      <div class="left-panel">
        <div class="chart-box">
          <h3>景区排行TOP10</h3>
          <div ref="rankChart" class="chart"></div>
        </div>
        <div class="chart-box">
          <h3>游客年龄分布</h3>
          <div ref="ageChart" class="chart"></div>
        </div>
      </div>

      <!-- 中间 -->
      <div class="center-panel">
        <div class="stat-cards">
          <div class="stat-item">
            <div class="label">今日游客量</div>
            <div class="value">{{ todayVisitors }}</div>
          </div>
          <div class="stat-item">
            <div class="label">今日评价数</div>
            <div class="value">{{ todayReviews }}</div>
          </div>
        </div>
        <div class="chart-box">
          <h3>客源地分布</h3>
          <div ref="mapChart" class="chart"></div>
        </div>
      </div>

      <!-- 右侧 -->
      <div class="right-panel">
        <div class="chart-box">
          <h3>游客趋势</h3>
          <div ref="trendChart" class="chart"></div>
        </div>
        <div class="chart-box">
          <h3>评价关键词</h3>
          <div ref="wordCloudChart" class="chart"></div>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.data-screen {
  width: 100vw;
  height: 100vh;
  background: #0a1e3e;
  color: #fff;
  overflow: hidden;
}

.screen-content {
  display: flex;
  height: calc(100vh - 80px);
  padding: 20px;
}

.left-panel, .right-panel {
  flex: 1;
}

.center-panel {
  flex: 2;
  margin: 0 20px;
}
</style>
```

---

## 四、状态管理 (Pinia Store)

### 4.1 用户状态管理

```javascript
// stores/user.js
import { defineStore } from 'pinia'
import { login, getUserInfo } from '@/api/user'

export const useUserStore = defineStore('user', {
  state: () => ({
    token: localStorage.getItem('token') || '',
    userInfo: null
  }),
  
  actions: {
    async login(loginForm) {
      const res = await login(loginForm)
      this.token = res.data.token
      this.userInfo = res.data.userInfo
      localStorage.setItem('token', this.token)
    },
    
    async getUserInfo() {
      const res = await getUserInfo()
      this.userInfo = res.data
    },
    
    logout() {
      this.token = ''
      this.userInfo = null
      localStorage.removeItem('token')
    }
  }
})
```

---

## 五、API封装

### 5.1 Axios配置

```javascript
// utils/request.js
import axios from 'axios'
import { ElMessage } from 'element-plus'
import { useUserStore } from '@/stores/user'

const service = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000
})

// 请求拦截器
service.interceptors.request.use(
  config => {
    const userStore = useUserStore()
    if (userStore.token) {
      config.headers['Authorization'] = `Bearer ${userStore.token}`
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器
service.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      ElMessage.error(res.msg || '请求失败')
      return Promise.reject(new Error(res.msg || '请求失败'))
    }
    return res
  },
  error => {
    ElMessage.error(error.message)
    return Promise.reject(error)
  }
)

export default service
```

### 5.2 API模块

```javascript
// api/crawler.js
import request from '@/utils/request'

// 启动爬取任务
export function startCrawl(data) {
  return request({
    url: '/api/crawler/start',
    method: 'post',
    data
  })
}

// 查询任务状态
export function getTaskStatus(taskId) {
  return request({
    url: `/api/crawler/status/${taskId}`,
    method: 'get'
  })
}

// 查询任务列表
export function getTaskList(params) {
  return request({
    url: '/api/crawler/tasks',
    method: 'get',
    params
  })
}
```

---

## 六、项目配置文件

### 6.1 package.json

```json
{
  "name": "tourism-vue",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.3.0",
    "vue-router": "^4.2.0",
    "pinia": "^2.1.0",
    "axios": "^1.4.0",
    "element-plus": "^2.3.0",
    "echarts": "^5.4.0",
    "dayjs": "^1.11.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.0",
    "vite": "^4.3.0",
    "eslint": "^8.42.0",
    "prettier": "^2.8.8"
  }
}
```

### 6.2 vite.config.js

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

---

**文档版本**：V1.0  
**编写日期**：2024-12-10  
**下一步**：数据爬取实现详细指南
